{
  "name": "blog-replica",
  "version": "1.0.0",
  "description": "A static blog replica (AtWill-style) deployed on GitHub Pages",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "serve": "python -m http.server 8080 --directory dist"
  }
}
