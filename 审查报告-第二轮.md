# 第二轮一比一复刻审查报告（复查 + 新发现）

- **审查日期**：2026-09-04（第二轮）
- **原站**：https://blog.imalan.cn/
- **复刻站**：https://anthony-hcy.github.io/Blog/
- **审查方式**：双站浏览器加载、模板/线上 HTML 对比、DOM 计算样式实测、交互点击验证
- **本轮重点**：用户指出的文章底部"上一篇/下一篇"（post-nav）区域问题，及全站复查
- **结论**：**发现 2 个确定性结构错误（post-nav / like-row），1 个顺带发现（空项残留文本），1 个细节差异（memo 标题格式）。上轮问题（图标、双重前缀、搜索分区、88x31）均已正常。**

---

## 🔴 严重

### S1. 文章底部"上一篇/下一篇"（post-nav）完全无样式
**用户指出的问题，已确认根因。**

| 对比项 | 原站 | 复刻站（线上实测） |
|---|---|---|
| 类名 | `.post-nav` 内是 **`nav-item`** / **`nav-label`** / **`nav-title`** | `post-nav-item` / `post-nav-label` / `post-nav-title` |
| 标签 | `<div class="nav-label">`、`<div class="nav-title">` | `<span>` |
| 计算样式 | `padding: 20px 0`，hover 变 accent 色，label 小号 mono 大写，title serif 14px | **`padding: 0px`**，无任何样式 |

- **证据 1**（原站 CSS `static/assets/main.css`）：只有 `.post-nav .nav-item { padding: 20px 0; ... }`、`.post-nav .nav-label`、`.post-nav .nav-title` 规则。
- **证据 2**（线上实测）：复刻站第一个 nav 链接计算样式 `padding: 0px`（原站应为 20px），`nav-item` 类无任何匹配规则，全部样式丢失。
- **证据 3**（复刻站生成源码）：
  ```html
  <nav class="post-nav"><a class="post-nav-item" href="/Blog/archives/weekend-log/">
    <span class="post-nav-label">Previous</span><span class="post-nav-title">周末生活志</span></a>
  <span class="post-nav-item is-empty"><span class="post-nav-label">Next</span></span></nav>
  ```
  原站应为：
  ```html
  <nav class="post-nav">
    <a class="nav-item" href="/archives/memo-20260720-1914/">
      <div class="nav-label">prev</div>
      <div class="nav-title">熊猫小A: 凌晨终场哨一响……</div>
    </a>
    <span class="nav-item"></span>
  </nav>
  ```

- **位置**：`scripts/build.mjs` 的 `postNav()` 函数（约 490-500 行）。

### S2. "Enjoyed this one?" 点赞行（article-like-row）结构不匹配
| 对比项 | 原站 | 复刻站 |
|---|---|---|
| 类名 | `article-like-row article-like-row-end article-like-row-surface`，内部 caption 用 `article-like-row-caption` | 只有 `article-like-row`，内部用 `like-hint`（**CSS 中无 `like-hint` 规则，字段为 0**） |
| 子元素顺序 | caption 在前 → like 按钮 → **浏览量**（共 3 个元素，含 pageview 计数） | like 按钮在前 → hint 在后（**缺浏览量计数**） |
| 效果 | 原站有上下边框、背景 padding、分隔效果 | 复刻站无边框/背景（缺 `-end`、`-surface` 类），纯裸排 |

- **位置**：`scripts/build.mjs` `buildPostPage()` 中的 `article-like-row` 区块。

---

## 🟡 中等

### M1. post-nav 空项（占位）残留 "Previous/Next" 文本
- 原站无该项时输出**空 span**：`<span class="nav-item"></span>`（无内容）。
- 复刻站输出：`<span class="post-nav-item is-empty"><span class="post-nav-label">Next</span></span>`——空位时仍显示 "Next"（或 "Previous"）文字。由于无 `is-empty` 对应样式且与其他项同网格，视觉上是"一条文字孤零零占一格"，非常显眼（memo-walk 页第一格就是这种情况）。
- **位置**：`postNav()` 中空项分支。

### M2. 空项 should 顺序颠倒时无影响，但文案应注意
- 原站 label 为小写 `prev` / `next`（CSS `text-transform: uppercase` 负责显示大写），复刻站直接写 `Previous` / `Next`。功能无碍，但若要 1:1 建议改成 `prev`/`next`（或保留大写，样式由 CSS 统一）。

### M3. memo 的 nav-title 格式差异
- 原站 memo 的 nav-title 显示 `作者: 摘要前60字`（如 "熊猫小A: 镰仓，江之岛和河口湖附近。……"）。
- 复刻站 memo nav-title 直接用 `post.title`（如 "周末散步随手拍"），无作者前缀、无摘要截断。**建议对齐原站为 `作者: 摘要` 格式**。

---

## ✅ 上轮问题复查（本轮确认已修复）

| 上轮问题 | 本轮验证结果 |
|---|---|
| 图标全灭 | ✅ 线上 22 个 `i.fa-*` 元素、FA CSS 引用正常、无 JS 残留 |
| 双重前缀 Blog/Blog | ✅ 品牌链接为 `https://anthony-hcy.github.io/Blog/`，RSS/atom 正常 |
| 搜索分区 (Untitled) | ✅ Posts/Pages/Categories/Tags 均显示真实名称 |
| 88x31 unavailable | ✅ 侧栏显示 4 张徽章 |
| Portal 资源路径 | ✅ 相对路径正常 |

---

## 📋 其他复查项（正常）

- 首页 feed（文章 + memo 混排）、归档、分类、标签、关于页：结构与类名对齐
- 搜索弹层、移动端菜单、返回顶部：交互正常
- 亮暗主题、字体（Google Fonts Chiron GoRound TC）：正常

---

## 🔧 修复建议（供修改模型参考，本轮未修改任何文件）

1. `postNav()`：类名改回 `nav-item` / `nav-label` / `nav-title`，标签用 `<div>`；空项输出**无内容的空 span** `<span class="nav-item"></span>`；label 文本改为小写 `prev`/`next`；memo 的 title 按 `作者: 摘要(60字)` 生成（现有 `excerptFrom` 可复用）。
2. like-row：类名补全 `article-like-row-end article-like-row-surface`；caption 改 `article-like-row-caption` 且**放在最前**；补齐浏览量计数（`index-metric-static pageview` 结构，参照文章页 meta 里的写法）；删除 `like-hint`。
3. 改完 `npm run build` 本地用 `/Blog` 前缀预览 → 对比原站截图确认样式 → push 自动部署。
