# 博客使用指南（新手版）

你的博客已部署在 GitHub Pages。下面是日常使用方法。

## 一、我的博客在哪

- 线上地址：**https://anthony-hcy.github.io/Blog/**
- 仓库地址：https://github.com/Anthony-hcy/Blog
- 每次推送到仓库的 `main` 分支，GitHub Actions 会自动重新构建并发布，等 1~2 分钟即可看到更新。

## 二、怎么发第一篇文章（两种方式）

### 方式 A：网页后台（Blog Portal，推荐，手机上也能用）

1. 打开 `https://anthony-hcy.github.io/Blog/portal.html`
2. 登录需要：
   - **Owner/Repo**：`Anthony-hcy/Blog`
   - **Personal Access Token**：需要你自己创建一个 GitHub Token
     - 打开 https://github.com/settings/tokens → Generate new token (classic)
     - 勾选 `repo` 权限（写仓库内容必须）
     - 生成后复制那串 `ghp_...` 粘贴进来（Token 只存在你自己浏览器里，安全）
3. 点 **Posts → New post** 写文章，点 **Save & Publish** 即提交，自动触发重建。

### 方式 B：直接在仓库里写（电脑上）

在仓库 `content/posts/` 目录新建 `.md` 文件：

```markdown
---
title: 我的第一篇文章
date: 2026-09-04 10:00:00+08:00
slug: my-first-post
type: post            # post=长文, memo=短篇（朋友圈式）
category: 日常
tags: [生活, 记录]
excerpt: 可选，列表页显示的摘要
banner: /assets/img/post/cover.png   # 可选封面图
draft: false          # true 则不发布
---

正文 Markdown…
```

保存后 push 到 main 分支即可。

## 三、怎么改站名、头像、简介

打开仓库根目录的 `site.config.json`：

```json
{
  "site": {
    "name": "你的站名",
    "subtitle": "一句话简介",
    "author": "你的名字",
    "url": "https://anthony-hcy.github.io/Blog",
    "base": "/Blog",
    "social": { "github": "https://github.com/你的名字", "weibo": "", "twitter": "" }
  }
}
```

- 站名：改 `name`
- 头像/Logo：替换仓库里 `site-root/logo.png`（180×180）和 `site-root/favicon.svg`
- 侧边栏社交链接：填 `social` 字段

## 四、怎么传图片

1. Portal 后台 **Images** 页签：填文章 slug → 选图片 → Upload
2. 图片会上传到 `static/assets/img/<slug>/`，得到引用：
   ```
   ![](/Blog/assets/img/<slug>/图片名.png)
   ```
3. 写文章时把这段引用粘贴进正文即可，图片会自动支持点击放大。

## 五、本地修改预览（可选）

需要电脑装 Node.js（已装）：

```bash
node scripts/build.mjs   # 生成 dist/（无需 npm install）
python -m http.server 8080 --directory dist   # 浏览器打开 http://localhost:8080
```

注意：本地预览地址没有 `/Blog` 前缀，若发现资源加载异常，可在 `site.config.json` 把 `base` 临时改成 `""`，构建后预览，改回 `"/Blog"` 再推送。

## 六、常见问题

| 问题 | 解决 |
|---|---|
| 改了内容线上没更新 | 检查仓库 Actions 页面是否有红叉；等 1~2 分钟 |
| Portal 登录后报 401 | Token 没有 `repo` 权限，重新生成勾选 |
| 分类/标签是中文乱码 | 目录名本来就是中文，正常 |
| 想换自己的域名 | 仓库 Settings → Pages → Custom domain |

## 七、技术说明（给想折腾的你）

- 构建脚本：`scripts/build.mjs`（Node + marked）
- 主题资源：`static/assets/`（源自 MIT 开源的 VOID/AtWill 主题结构）
- 搜索：ExSearch（本地 JSON 索引，构建时自动生成）
- 公式：KaTeX；图片画廊：PhotoSwipe
- 无后端、无数据库，全静态，0 元托管
