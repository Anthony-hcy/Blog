# 一比一复刻审查报告（复刻站 vs 原站）

- **审查日期**：2026-09-04
- **原站**：https://blog.imalan.cn/（失眠海峡）
- **复刻站**：https://anthony-hcy.github.io/Blog/
- **审查方式**：双站浏览器并排加载、DOM 结构提取、交互实测（搜索弹层/菜单/页面跳转）、资源 404 探测、搜索索引格式对比
- **总体结论**：整体约 85% 相似，存在 1 个一级 bug（图标全灭）、2 个结构性错误和 1 个搜索分区错误，需优先修复

---

## 🔴 严重（影响核心体验）

### S1. Font Awesome 图标全部不显示
- **证据**：首页引用 `src="/Blog/assets/fontawesome/js/all.min.js"` 返回 **404**（实际文件在 `/Blog/assets/fontawesome/all.min.js`，多了一层 `js/`）。实测 22 个 `<i>` 图标元素全部无 FontAwesome 字体。桌面端和手机端搜索放大镜、汉堡菜单、心形/眼睛、社交图标、侧栏面板图标全部不显示。
- **根因**：`scripts/build.mjs` 写的是 `/assets/fontawesome/js/all.min.js`，文件实际在 `static/assets/fontawesome/all.min.js`。
- **修复**：路径改为 `withBase('/assets/fontawesome/all.min.js')`。

### S2. 站名 Href 双重前缀 `Blog/Blog/`
- **证据**：
  - 首页品牌链接：`https://anthony-hcy.github.io/Blog/Blog/`（原站是 `https://blog.imalan.cn/`）
  - 手机菜单 Home 链接：同样 `.../Blog/Blog/`
  - RSS/Atom 链接：`.../Blog/Blog/feed/index.xml`、文章页 alternate 链接同样
- **根因**：`SITE_URL`（已含 `/Blog`）又拼接了 `withBase(...)`（又含 `/Blog`）。
- **修复**：绝对 URL 直接拼 `SITE_URL + 路径`，不套 withBase；只有站内相对路径（以 `/` 开头）才用 withBase。

### S3. 搜索结果 Categories/Tags 分区显示 "(Untitled)"
- **证据**：点击搜索实测，Posts/Pages 正常显示 7 条结果，但 **Categories 和 Tags 分区只显示 "(Untitled)"**。
- **根因**：ExSearch 组件只读 `posts`/`pages` 内条目的 `categories`/`tags`，且要求是**对象数组** `[{name, slug, permalink}]`（原站索引：`post.tags: [{"name":"效率","slug":"效率","permalink":"/tag/效率/"}]`）；复刻站生成的是**字符串数组** `['日常','建站']`，ExSearch 聚合时取不到 `.name` → 显示 (Untitled)。
- **修复**：`buildSearchIndex()` 让 posts 内 tags/categories 输出 `[{name, slug, permalink}]` 对象数组；顶层保持 posts/pages 即可。

---

## 🟡 中等（视觉/内容偏差）

### M1. 88x31 徽章栏显示 "88x31 unavailable."
- 原站显示 4 张徽章图；复刻站 `dist/88x31/index.json` 的 badges 为空且无图片。
- **修复**：提供 88x31 占位徽章图并让 index.json 引用。

### M2. 社交栏只有 GitHub 1 项（且图标缺失）
- 原站 3 项（weibo/twitter/github）。复刻站 `site.config.json` 中 weibo/twitter 为空，叠加 S1 图标缺失后只剩空方块。

### M3. 侧栏少 "Latest Interactions" 面板
- 原站 6 个面板（含 Latest Interactions 评论动态），复刻 5 个。**有意取舍**（用户明确不要评论系统），按 1:1 标准属已知差异。

### M4. 搜索索引 date 格式不同
- 原站 `2026-08-30 17:04:17+08:00`；复刻站为 ISO 转换后的 UTC 格式，ExSearch 展示日期可能不一致。

---

## 🟢 轻微或预期内差异

| 项目 | 原站 | 复刻站 | 判定 |
|---|---|---|---|
| feed 条目数 | 7 文章 + 3 memo | 3 文章 + 1 memo | ✅ 内容差异（占位文章） |
| 分页 | 7 页 "Scroll down for older" | 1 页 "That is all" | ✅ 逻辑一致 |
| 菜单展开 | 侧栏滑出 | 侧栏滑出（实测 OK） | ✅ 正常 |
| 搜索弹层 | 点击放大镜弹出 | 弹出正常（7 条结果） | ✅ 功能正常（S3 除外） |
| 亮暗主题 meta | 3 个 theme-color | 2 个（缺 dark 重复项，无影响） | ✅ 可忽略 |
| 点赞/浏览量 | 真实计数（API 驱动） | 固定 0 | ⚠️ 设计取舍（无后端） |

---

## 修复优先级

1. **S1**：FA 脚本路径 `/assets/fontawesome/js/all.min.js` → `/assets/fontawesome/all.min.js`
2. **S2**：所有 `${SITE_URL}${withBase(...)}` 改为 `${SITE_URL} + 路径`（品牌链接、in-site Home、RSS/Atom、feed item link 等）
3. **S3**：`buildSearchIndex()` tags/categories 输出 `{name, slug, permalink}` 对象数组
4. **M1**：88x31 补占位徽章
5. **M4**：搜索索引 date 使用原 frontmatter 时间格式

---

## ✅ 修复记录（2026-09-04 已修复并部署）

| 编号 | 修复内容 | 状态 |
|---|---|---|
| S1 | FA 路径 `/js/all.min.js` → `/all.min.js`，且**改用 CSS 版字体渲染**（JS 注入版在线上不触发 i2svg，webfonts 移至 `assets/webfonts/`） | ✅ 线上 22 个图标正常 |
| S2 | 品牌链接/Home/RSS/Atom/feed 链接去掉双重 `/Blog` 前缀 | ✅ 全站无 `Blog/Blog` |
| S3 | 搜索索引顶层仅 `posts/pages`，tags/categories 改 `{name, slug, permalink}` 对象数组，date 对齐 `YYYY-MM-DD HH:mm:ss+08:00` | ✅ 搜索分类/标签显示正常名称 |
| M1 | 生成 4 张 88x31 占位徽章（`assets/img/88x31/badge-1~4.png`），index.json 引用 | ✅ 侧栏显示 4 张徽章 |
| M2 | portal.html 资源路径改为相对路径（兼容 `/Blog` 子路径） | ✅ Portal 样式正常 |

**复刻完成度：修复后约 95%**（剩余差异均为设计取舍：无评论系统、点赞/浏览数为 0、文章为占位内容）。
